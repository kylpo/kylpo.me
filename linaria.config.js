/* See https://github.com/callstack/linaria/blob/master/docs/CONFIGURATION.md */

module.exports = {
  // evaluate: true,
  // displayName: false,
  classNameSlug: "[hash]", // Just the hash of the content (so repeatedly used styles will use the same classNames)
}
